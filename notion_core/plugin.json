{
  "tesserae_compat": "1.x",
  "name": "Notion Core",
  "version": "0.3.1",
  "kind": "data",
  "description": "Shared Notion connection for the notion_* widget family. Add one or more Notion accounts, each with a friendly name and its own integration token, on this plugin's admin page. Lists the databases each account can see and the property mapping each widget will use. No cell of its own.",
  "icon": "ph-notion-logo",
  "supports": {
    "sizes": [
      "xs",
      "sm",
      "md",
      "lg"
    ]
  },
  "requires": [
    "network:api.notion.com",
    "settings:plugin"
  ],
  "render": {
    "needs_network": true
  }
}
